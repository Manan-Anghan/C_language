#include<stdio.h>
#include<conio.h>

main()

{
    char x;
    clrscr();
    printf("enter the electricity bill unit :");
    scanf("%c",&x);

    if(a<=50)
    {
       if(a<=150)
       {
	  if(a<=250)
	  {
	     if(a>=250)
	     {
		printf("the unit price will increase 1.50/unit");
	     }
	     else
	     {
		printf("the unit price will increase 1.20/unit");
	     }

	  }
	  else
	  {
	     pirntf("the unit price will increase ");
	  }
       }
       else
       {

       }
    }
    else
    {

    }

}