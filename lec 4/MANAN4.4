#include<stdio.h>
#include<conio.h>

main()

{
    int A,a,b;
    clrscr();
    printf("enter the value of a :");
    scanf("%d",&a);
    printf("enter the value of b :");
    scanf("%d",&b);
    A=(a*a*a)-(b*b*b)-3*(a*a*b)+3*(a*b*b);
    printf("the formula of (a-b)^3 is :%d", A);

    getch();

}