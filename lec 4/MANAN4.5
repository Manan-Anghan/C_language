#include<stdio.h>
#include<conio.h>

main()

{
    int A,a,b,c;
    clrscr();
    printf("enter the value of a :");
    scanf("%d",&a);
    printf("enter the value of b :");
    scanf("%d",&b);
    printf("enter the value of c :");
    scanf("%d",&c);
    A=(a*a*a)+(b*b*b)+(c*c*c)+3*(a+b)*(a+c)*(b+c);
    printf("the formula of (a+b+c)^3 is :%d", A);
    getch();
}
